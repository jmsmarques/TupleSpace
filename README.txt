.NET 
Tuple Space

Jorge Marques
Francisco Quinaz
Miguel Reis

Server is configured in serverConf.txt
 ViewPort and ViewServer are the port and ip respectively of another server in the network
 Put the ViewServer as null if it-s the first server of the network

Client is configured in clientConf.txt
 Port and Server are the port and ip respectively of a server in the network

After the configuration is finished
1.Run at least 1 server
2.Run more servers or client

Careful with the cofigurations

When the client starts it expects a name of a file with inputs